# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# naming
- Do not use `graphify` as a shorthand alias for `graphify-diff` — it conflicts with the existing Graphify tool's command name. Confidence: 0.70

# cli
- For CLI tools operating on git repos, auto-detect the repo and infer smart defaults (e.g., diff range since last graph build) instead of requiring explicit path and flag arguments when the user is already inside the repo. Confidence: 0.70

